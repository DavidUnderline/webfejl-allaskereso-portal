import { CompanycurrencyPipe } from './companycurrency.pipe';

describe('CompanycurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanycurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
